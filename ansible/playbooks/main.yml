---
- name: Complete Django Application Deployment Pipeline
  hosts: localhost
  gather_facts: yes

  tasks:
    - name: Step 1 - Install dependencies
      import_playbook: install_dependencies.yml

    - name: Step 2 - Build Docker image
      import_playbook: automate_docker.yml

    - name: Step 3 - Deploy to Kubernetes
      import_playbook: deploy_kubernetes.yml
